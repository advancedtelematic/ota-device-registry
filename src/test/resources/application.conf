database = {
  properties = {
    user = "sota_test"
    user = ${?DB_USER}
    password = "s0ta"
    password = ${?DB_PASSWORD}
  }
  catalog = "device_registry_ptest"
}

test {
  random {
    minSuccessful = 100
    minSuccessful = ${?TEST_RANDOM_MINSUCCESSFUL}
  }
}

messaging.mode = "test"
