# Device Registry Chart

Helm chart for device registry. Use by first running:

```
cp secret-values.yaml.example secret-values.yaml
```

Edit the `secret-values.yaml` file, then run:

```
helm install -f secret-values.yaml .
```
